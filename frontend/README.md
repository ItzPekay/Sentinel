# Sentinel — Frontend

Next.js 14 (App Router) frontend for the Sentinel stroke detection app.

See the root [README](../README.md) for the full project overview and setup instructions.

```bash
npm install
npm run dev   # http://localhost:3000
```

Requires `frontend/.env.local` with `NEXT_PUBLIC_API_URL=http://localhost:8001`.
